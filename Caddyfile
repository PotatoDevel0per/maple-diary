# maplediary.site — Let's Encrypt 인증서 자동 발급/갱신
# (도메인 A레코드가 이 PC 공인 IP를 가리키고 80/443 포트포워딩이 되어 있어야 발급됨)
maplediary.site {
	encode gzip zstd
	reverse_proxy maple-diary:3000
}

# 앞으로 앱을 추가하면 이렇게 블록을 늘린다:
# app2.example.com {
# 	reverse_proxy app2:3000
# }
